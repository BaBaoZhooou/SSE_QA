# fastQA Prompts — 固态电解质领域

运行时 LLM 提示词目录。修改 `.txt` / `intent_tags.json` 后需重启 fastQA 服务。

环境变量 `PROMPTS_DIR` 或 `FASTQA_PROMPTS_DIR` 可指向其它目录；默认本目录 `fastQA/prompts/`。

换域指南见项目根目录 `docs/DOMAIN_SETUP.md` 第 5.1 节。
